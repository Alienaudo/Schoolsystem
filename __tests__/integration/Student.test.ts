import { describe, test, beforeAll, afterAll } from "vitest";

describe('Test de integração Student', () => {

    beforeAll(() => { });

    test('', async () => { });

    afterAll(() => { });

});
